services:
  redis:
    image: redis:7-alpine
    container_name: redis-from-scratch
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

  mongodb:
    image: mongo:7
    container_name: mongodb-from-scratch
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: redis_from_scratch
    volumes:
      - mongo-data:/data/db

volumes:
  redis-data:
  mongo-data:
